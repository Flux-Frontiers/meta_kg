feat(cli): Invert build wipe behavior; add metakg-update command

Breaking change: metakg-build now wipes existing database by default (safer).
Use --no-wipe flag to add files incrementally instead.

Added:
- New metakg-update command for explicit incremental updates
- Updated --wipe flag to --no-wipe (inverted logic, default=True)

Updated documentation (CLAUDE.md, README.md, WORKFLOW.md) to reflect
new defaults and usage patterns.

Also includes:
- Article compilation artifacts (PDF, bibliography files)
- CLI option refactoring for clarity with inverted flags
